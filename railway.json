{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm --filter @lcos/core build && pnpm --filter @lcos/api prisma:generate && pnpm --filter @lcos/api build"
  },
  "deploy": {
    "startCommand": "pnpm --filter @lcos/api exec prisma migrate deploy && node apps/api/dist/main.js",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
